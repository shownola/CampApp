<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Camp App</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/main.css">
  </head>
  <body>

    <nav class="navbar navbar-expand-lg navbar-light bg-custom mb-2">
      <div class="container-fluid">
        <a href="/" class="navbar-brand">CampApp</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav ml-auto">
          <% if(!currentUser){ %>
            <li class="nav-item pl-3"><a class="nav-link" href="/login">Login</a></li>
            <li class="nav-item pl-3"><a class="nav-link" href="/register">Sign Up</a></li>
          <% } else { %>
            <li><a>Signed In As: <i><b><%= currentUser.username %></i></b></a></li>
            <li class="pl-3"><a href="/logout">Logout</a></li>
          <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <% if(error && error.length > 0){ %>
        <div class="alert alert-danger" role='alert'>
          <%= error %>
        </div>
      <% } %>
      <% if(success && success.length > 0){ %>
        <div class="alert alert-success" role='alert'>
          <%= success %>
        </div>
      <% } %>
    </div>


    <div class="container">
